import { userName, greeting } from "./feature1.js";
import { userGreeting } from "./feature2".js;
// invoke using userGreeting and pass userName and greeting as arguments
userGreeting(userName, greeting)